package com.jianqing.interview.facebook.lc20;

/**
 * 可以用两个hashmap
 *
 * loop s 的时候， 如果当前字符在s map里， t当前字符就得match s map里面记录的
 * 如果不在s map 里， 就放进去s map， 然后检查t map，t map里对应的当前t 的字符也应该是s，否则return false
 *
 * 参考 IsomorphicStrings_205
 */
public class LC_205_IsomorphicStrings {
}
